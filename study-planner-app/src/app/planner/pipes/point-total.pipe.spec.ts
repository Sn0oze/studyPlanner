import { PointTotalPipe } from './point-total.pipe';

describe('PointTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new PointTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
