import { ActiveYearPipe } from './active-year.pipe';

describe('ActiveYearPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveYearPipe();
    expect(pipe).toBeTruthy();
  });
});
