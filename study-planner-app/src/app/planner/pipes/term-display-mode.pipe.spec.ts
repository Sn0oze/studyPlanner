import { TermDisplayModePipe } from './term-display-mode.pipe';

describe('TermDisplayModePipe', () => {
  it('create an instance', () => {
    const pipe = new TermDisplayModePipe();
    expect(pipe).toBeTruthy();
  });
});
