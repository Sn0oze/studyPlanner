import {TermDisplayModeHintPipe} from './term-display-mode-hint.pipe';

describe('TermDisplayModeLabel', () => {
  it('create an instance', () => {
    const pipe = new TermDisplayModeHintPipe();
    expect(pipe).toBeTruthy();
  });
});
